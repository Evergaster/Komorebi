"""Komorebi internal modules.

This project historically imported modules as `src.<module>`.
Keeping `src` as an importable package makes both `python main.py` and
PyInstaller onefile execution work consistently.
"""
